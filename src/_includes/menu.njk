---
title: Dog Foods
---

{% import "components.njk" as c%}
{%extends "base.njk"%}
{% block css %}
    <link rel="stylesheet" href="/site/assets/css/sidebar.css">
    {# <style type="text/css">
        a[class="active"]:focus ~ li:not([class="adult"]) {
        display:none;
        
    </style> #}
{% endblock %}

{% macro extract_data(products) %}
    <ul>
        <li>---</li>
        <li>products: </li>
        {% for product in products %}
        <li>- id: {{product.product_code}}</li>
        <li>&nbsp;&nbsp;name: {{product.name}}</li>
        <li>&nbsp;&nbsp;short_description: {{product.short_description}}</li>
        <li>&nbsp;&nbsp;price: {{product.price}}</li>
        {% endfor %}
        <li>---</li>
    </ul>
{% endmacro %}

{% block container %}


{{c.hero_section(hero, '../')}}
  
  {# <a class="btn btn-primary" data-bs-toggle="collapse" href="#all" role="button" aria-expanded="false" aria-controls="collapseExample">
    Link with href
  </a>
  <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#all" aria-expanded="false" aria-controls="collapseExample">
    Button with data-bs-target
  </button> #}

  
    <div class="container text-center">
        <div class="row">
            {# <button class="active btn btn-primary" style="height:5rem; width: 100%;">
                Link with href
            </button> #}
            {% for product in products %}
                {# {% for product.tag in product.tags %} #}
                    {% include "item.njk" %}
                {# {% endfor %} #}
            {% endfor %}
        </div>
    </div>
{% endblock %}

{% block js %}
    <script src="sidebar.js">
        /* global bootstrap: false */
        (() => {
        'use strict'
        const tooltipTriggerList = Array.from(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
        tooltipTriggerList.forEach(tooltipTriggerEl => {
            new bootstrap.Tooltip(tooltipTriggerEl)
        })
        })()

    </script>
{% endblock %}

    